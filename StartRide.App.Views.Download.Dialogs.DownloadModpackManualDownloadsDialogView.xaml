<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="StartRide.App.Views.Download.Dialogs.DownloadModpackManualDownloadsDialogView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:download="clr-namespace:StartRide.App.ViewModels.Download" xmlns:res="clr-namespace:StartRide.App.Resources" Focusable="False" xmlns:dialogs="clr-namespace:StartRide.App.Views.Download.Dialogs">
  <Grid Style="{StaticResource DialogContentGridStyle}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <StackPanel>
      <TextBlock Style="{StaticResource DialogTitleTextStyle}" Text="{Binding ModpackManualDownloadsDialog.Title}" />
      <TextBlock Style="{StaticResource DialogSubtitleTextStyle}" Text="{Binding ModpackManualDownloadsDialog.Message}" />
      <TextBlock Margin="0,10,0,0" Style="{StaticResource DialogBodyTextStyle}" Text="{Binding ModpackManualDownloadsDialog.Hint}" />
    </StackPanel>
    <Border Grid.Row="1" Margin="0,18,0,0" Style="{StaticResource ReadOnlyFieldSurfaceStyle}">
      <ScrollViewer MaxHeight="260" VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding ModpackManualDownloadsDialog.Files}">
          <ItemsControl.ItemTemplate>
            <DataTemplate DataType="{x:Type download:DownloadModpackManualDownloadItemViewModel}">
              <StackPanel Margin="0,0,0,12">
                <TextBlock Style="{StaticResource DialogFieldLabelTextStyle}" Text="{Binding Title}" />
                <TextBlock Margin="0,4,0,0" Style="{StaticResource DialogBodyTextStyle}" Text="{Binding FileName}" />
                <TextBlock Margin="0,4,0,0" Style="{StaticResource DialogValidationTextStyle}" Text="{Binding FailureSummary}" />
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </Border>
    <StackPanel Grid.Row="3" Margin="0,24,0,0" Style="{StaticResource DialogFooterStyle}">
      <Button Style="{StaticResource LauncherDialogButtonStyle}" Content="{x:Static res:Strings.Dialog_OpenFileButton}" Command="{Binding ModpackManualDownloadsDialog.OpenFileCommand}" />
      <Button Style="{StaticResource LauncherPrimaryDialogButtonStyle}" Margin="{StaticResource DialogTrailingButtonMargin}" Content="{x:Static res:Strings.Confirm_Button}" Command="{Binding ModpackManualDownloadsDialog.CloseCommand}" />
    </StackPanel>
  </Grid>
</UserControl>