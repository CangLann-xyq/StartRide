<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Launcher.App.Views.Multiplayer.CreateLobbyView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Launcher.App.Controls" xmlns:res="clr-namespace:Launcher.App.Resources" Focusable="False" xmlns:multiplayer="clr-namespace:Launcher.App.Views.Multiplayer">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/StartRide;component/Views/Settings/SettingsSectionResources.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Name="CreateLobbyStepHost" ClipToBounds="True">

    <!-- ============ 开房前：页头 → 步骤卡面 → 主操作 ============ -->
    <StackPanel Name="CreateLobbySetupLayer" Margin="12,62,12,72" MaxWidth="{StaticResource DetailPageContentMaxWidth}" HorizontalAlignment="Stretch">
      <UIElement.RenderTransform>
        <TranslateTransform />
      </UIElement.RenderTransform>

      <!-- ① 页头 -->
      <DockPanel LastChildFill="False" Margin="0,0,0,14">
        <StackPanel Orientation="Horizontal">
          <controls:SvgIcon Width="22" Height="22" Margin="0,0,11,0" VerticalAlignment="Center" IconKey="main_menu_multiplayer" Foreground="{DynamicResource Brush.Text.Strong}" />
          <TextBlock Style="{StaticResource RidePageTitleTextStyle}" Text="{x:Static res:Strings.Multiplayer_SectionCreateLobby}" />
          <TextBlock Style="{StaticResource RidePageSubtitleTextStyle}" Text="开房后把房间码发给朋友，他们输码即可进入" />
        </StackPanel>
      </DockPanel>

      <!-- ② 说明 + 步骤：换成带序号的规整步骤，不再是一段流水文字 -->
      <Border Style="{StaticResource RideDetailSurfaceStyle}">
        <StackPanel>
          <TextBlock Text="{x:Static res:Strings.Multiplayer_Create_OneClickHint}" Foreground="{DynamicResource Brush.Text.Primary}" FontSize="13" LineHeight="21" TextWrapping="Wrap" />
          <Border Style="{StaticResource RideHairlineStyle}" />
          <StackPanel Orientation="Horizontal">
            <Border Width="22" Height="22" CornerRadius="11" Background="#1FFFFFFF" VerticalAlignment="Top">
              <TextBlock Text="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Muted}" />
            </Border>
            <TextBlock Margin="11,1,0,0" Text="{x:Static res:Strings.Multiplayer_Create_StepOpenToLan}" Foreground="{DynamicResource Brush.Text.Muted}" FontSize="12.5" LineHeight="20" TextWrapping="Wrap" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Border Width="22" Height="22" CornerRadius="11" Background="#1FFFFFFF" VerticalAlignment="Top">
              <TextBlock Text="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Muted}" />
            </Border>
            <TextBlock Margin="11,1,0,0" Text="{x:Static res:Strings.Multiplayer_Create_StepSelectInstance}" Foreground="{DynamicResource Brush.Text.Muted}" FontSize="12.5" LineHeight="20" TextWrapping="Wrap" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Border Width="22" Height="22" CornerRadius="11" Background="#1FFFFFFF" VerticalAlignment="Top">
              <TextBlock Text="3" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Muted}" />
            </Border>
            <TextBlock Margin="11,1,0,0" Text="{x:Static res:Strings.Multiplayer_Create_StepCreateLobby}" Foreground="{DynamicResource Brush.Text.Muted}" FontSize="12.5" LineHeight="20" TextWrapping="Wrap" />
          </StackPanel>
        </StackPanel>
      </Border>

      <!-- ③ 主操作 -->
      <Button Margin="0,18,0,0" HorizontalAlignment="Left" MinWidth="168" Content="{x:Static res:Strings.Multiplayer_Create_OneClickButton}" Style="{StaticResource LauncherPrimaryDialogButtonStyle}" Command="{Binding CreateLobbyCommand}" />
    </StackPanel>

    <!-- ============ 开房后：页头 → 房间码卡 → 成员列表 ============ -->
    <Grid Name="CreatedLobbyLayer" MinHeight="360" Opacity="0" Visibility="Collapsed">
      <UIElement.RenderTransform>
        <TranslateTransform />
      </UIElement.RenderTransform>
      <StackPanel Margin="12,62,12,72" MaxWidth="{StaticResource DetailPageContentMaxWidth}" HorizontalAlignment="Stretch">

        <!-- ① 页头（图标按实际资源名小写，原版写成 main_menu_Multiplayer 是取不到图的） -->
        <DockPanel LastChildFill="False" Margin="0,0,0,14">
          <Button DockPanel.Dock="Right" VerticalAlignment="Center" Style="{StaticResource LauncherDangerDialogButtonStyle}" Content="{Binding LeaveLobbyButtonText}" Command="{Binding RequestLeaveLobbyCommand}" />
          <StackPanel Orientation="Horizontal">
            <controls:SvgIcon Width="22" Height="22" Margin="0,0,11,0" VerticalAlignment="Center" IconKey="main_menu_multiplayer" Foreground="{DynamicResource Brush.Text.Strong}" />
            <TextBlock Style="{StaticResource RidePageTitleTextStyle}" Text="{Binding LobbyTitle}" />
          </StackPanel>
        </DockPanel>

        <!-- ② 房间码：这是本页最该被一眼看到的信息，单独做成大号卡片 -->
        <Border Style="{StaticResource RideDetailSurfaceStyle}" Margin="0,0,0,14">
          <DockPanel LastChildFill="False">
            <Button DockPanel.Dock="Right" VerticalAlignment="Center" Style="{StaticResource LauncherDialogButtonStyle}" Content="复制房间码" Command="{Binding CopyRoomCodeCommand}" />
            <StackPanel>
              <TextBlock Text="{x:Static res:Strings.Multiplayer_LobbyRoomCodeHeader}" FontSize="11.5" Foreground="{DynamicResource Brush.Text.Subtle}" />
              <TextBlock Margin="0,6,0,0" Text="{Binding RoomCode}" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource Brush.Accent.Primary}" />
            </StackPanel>
          </DockPanel>
        </Border>

        <!-- ③ 成员列表 -->
        <Border Style="{StaticResource RideListSurfaceStyle}" MinHeight="160">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Margin="12,8,12,6" LastChildFill="False">
              <TextBlock Text="{x:Static res:Strings.Multiplayer_LobbyPlayerListHeader}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Muted}" />
              <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" FontSize="11.5" Foreground="{DynamicResource Brush.Text.Subtle}" Text="{x:Static res:Strings.Multiplayer_LobbyPoweredByTerracotta}" />
            </DockPanel>
            <ItemsControl Grid.Row="1" Margin="4,0,4,6" ItemsSource="{Binding LobbyPlayers}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <controls:ListPageItemButton IconKey="main_menu_account" ItemMargin="0" Title="{Binding DisplayName}" Subtitle="{Binding Subtitle}" TrailingText="{Binding LatencyText}" IsFirstVisible="{Binding IsFirst}" IsLastVisible="{Binding IsLast}">
                    <controls:ListPageItemButton.TitleTrailingContent>
                      <StackPanel Orientation="Horizontal">
                        <controls:AdaptiveTagList ItemsSource="{Binding RoleTags}">
                          <FrameworkElement.Style>
                            <Style TargetType="{x:Type controls:AdaptiveTagList}">
                              <Style.Triggers>
                                <DataTrigger Value="True" Binding="{Binding IsHost}">
                                  <Setter Property="controls:AdaptiveTagList.TagBackground" Value="{DynamicResource Brush.Accent.Selection}" />
                                  <Setter Property="controls:AdaptiveTagList.TagForeground" Value="{DynamicResource Brush.Accent.Primary}" />
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </FrameworkElement.Style>
                        </controls:AdaptiveTagList>
                        <controls:AdaptiveTagList TagBackground="{DynamicResource Brush.Accent.Selection}" TagForeground="{DynamicResource Brush.Accent.Primary}" ItemsSource="{Binding LocalTags}" />
                      </StackPanel>
                    </controls:ListPageItemButton.TitleTrailingContent>
                  </controls:ListPageItemButton>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </Grid>
        </Border>
      </StackPanel>
    </Grid>
  </Grid>
</UserControl>
