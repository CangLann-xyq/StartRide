<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Launcher.App.Views.Account.AccountMenuView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:accountViewModels="clr-namespace:Launcher.App.ViewModels.Account" xmlns:controls="clr-namespace:Launcher.App.Controls" xmlns:views="clr-namespace:Launcher.App.Views.Account" xmlns:res="clr-namespace:Launcher.App.Resources" Focusable="False">
  <StackPanel>
    <ItemsControl ItemsSource="{Binding AccountList.Accounts}">
      <ItemsControl.ItemTemplate>
        <DataTemplate DataType="{x:Type accountViewModels:AccountItemViewModel}">
          <Grid>
            <controls:SecondaryMenuOptionButton TextMargin="4,0,42,0" Text="{Binding DisplayName}" IsSelected="{Binding IsSelected}" IsExternalMouseOver="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}" Command="{Binding DataContext.AccountList.SelectAccountCommand, RelativeSource={RelativeSource AncestorType={x:Type views:AccountMenuView}}}" CommandParameter="{Binding}">
              <!-- StartRide：有真实头像（Steam 导入）时显示头像图，否则回退通用人形 SVG -->
              <FrameworkElement.Style>
                <Style TargetType="{x:Type controls:SecondaryMenuOptionButton}">
                  <Setter Property="IconMode" Value="Svg" />
                  <Setter Property="IconKey" Value="general/general_person" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding HasAvatar}" Value="True">
                      <Setter Property="IconMode" Value="Avatar" />
                      <Setter Property="AvatarSource" Value="{Binding AvatarSource}" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </FrameworkElement.Style>
            </controls:SecondaryMenuOptionButton>
            <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,6" Command="{Binding DataContext.RequestDeleteAccountCommand, RelativeSource={RelativeSource AncestorType={x:Type views:AccountMenuView}}}" CommandParameter="{Binding}">
              <FrameworkElement.Style>
                <Style BasedOn="{StaticResource SecondaryMenuDeleteButtonStyle}" TargetType="{x:Type Button}">
                  <Setter Property="UIElement.Opacity" Value="0" />
                  <Setter Property="UIElement.IsHitTestVisible" Value="False" />
                  <Style.Triggers>
                    <DataTrigger Value="True" Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}">
                      <Setter Property="UIElement.Opacity" Value="1" />
                      <Setter Property="UIElement.IsHitTestVisible" Value="True" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </FrameworkElement.Style>
              <controls:SvgIcon Width="16" Height="16" IconKey="general/general_delete" Style="{StaticResource DangerActionSvgIconStyle}" />
            </Button>
          </Grid>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <controls:SecondaryMenuOptionButton Text="{x:Static res:Strings.Account_AddButton}" IconMode="Svg" IconKey="account_page/account_page_add_account" IconWidth="24" IconHeight="24" Command="{Binding RequestAddAccountCommand}" />
  </StackPanel>
</UserControl>